# Hybrid Deployment Environment Variables

# Render Authentication Service (hosted on Render)
# This service handles login, registration, and user management
VITE_RENDER_AUTH_URL=https://your-render-app-name.onrender.com
VITE_RENDER_AUTH_API_KEY=your_render_auth_api_key_here

# Company Translation Backend (hosted on company server)
# This service handles document translation and processing
VITE_TRANSLATION_BACKEND_URL=http://your-company-server:5000
VITE_TRANSLATION_API_KEY=your_translation_api_key_here

# Legacy variables for backward compatibility
VITE_API_URL=http://your-company-server:5000

# Instructions:
# 1. Replace the placeholder URLs with your actual Render app URL and company server URL
# 2. Update API keys with your actual keys
# 3. For local development, you can use localhost URLs
# 4. For production deployment, use your actual hosted URLs